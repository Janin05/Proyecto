import 'package:flutter/material.dart';

class ViaticosPage extends StatelessWidget {
  const ViaticosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Control de viáticos')),
      body: const Center(child: Text('Pantalla de Viáticos')),
    );
  }
}
